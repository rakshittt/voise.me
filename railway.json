{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/api && uv sync && uv run python -m spacy download en_core_web_sm"
  },
  "deploy": {
    "startCommand": "cd apps/api && uv run alembic upgrade head && uv run uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}
